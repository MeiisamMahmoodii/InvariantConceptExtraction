# Current figure data

- `figure3_factor_stability.json` supplies the validation-selected activation example and aggregate stability panel.
- `figure4_feature_examples.json` supplies the held-out intent-feature example cards.

Figures 1 and 2 are generated from the frozen method configuration and current summary CSV files in `Report/`.
